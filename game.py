import new_character #imports new_character class
import os.path 
check_for_file = os.path.isfile("player_profile.txt")) #returns true or false if file does or doesn't exit
if check_for_file == True:
    
print("Hello and Welcome")
